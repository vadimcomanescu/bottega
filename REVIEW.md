# Reviewing bottega

Bottega is a method in markdown; most defects are false claims, drifted duplicates, and register violations, not crashing code.

- Worker model choices live in one table, `skills/maestro/references/workers.md`, one row per worker with one model and one effort; the phases name the worker they dispatch and nothing else. Maestro states the orchestrator model and the rules binding every choice, the review engines are named in `skills/code-review/references/autoreview.md`, and the panel's seats in `skills/panel`. A second home for any of these is a defect.
- Enforcement is one rule in `hooks/route-guard.mjs`: a session owning a live run names a model on every subagent, workflow, and codex dispatch, never fable. Guard changes need `tests/route-guard.test.ts` to still pass against real event shapes.
- There are no agent identity files. Worker method arrives per dispatch from skills (`implementing`, `qa`).
- The skills tree is the product and must stay harness-portable: nothing in `skills/` may assume slash commands, subagents, or a plugin root exists, except where a harness is named.
- The same tree must stay self-contained: nothing in `skills/` references this repository's own record (`docs/`, `REVIEW.md`, `tests/`), which does not install with the plugin. A path a skill names either resolves inside `skills/`, names a component the plugin ships, or names a file of the host repository the run works on.
- The plan editor, the review engines, and the panel's codex seat are fresh CLI contexts: workers, but not subagent dispatches, and subagent dispatches are what the guard inspects. The panel's claude seat and its judge are subagents, so the guard does read them, and they pass by naming opus-5.
- Reviewing this repo's prose means judging the diff as skill writing: held to the skill-writing doctrine (leading words, no-ops, duplication, sediment), the register, consistency of the map and cross-references, and the logical flow of the method (every route names an action that resolves; every term has one definition). A read of the same diff by another company's model is welcome; the autoreview engine is not the tool for prose, by its own contract. On a host repo, a run's integrated review always runs both Claude and GPT through the vendored autoreview helper; the codex CLI is a hard requirement for it.
- Every claim in AGENTS.md, README.md, and skill cross-references must be true of the tree in the same diff that changes the tree.
- Register: plain engineering English, no em dashes, no "bearing"/"ledger", one route verb ("use").
- Over-specification is a defect: a worker instruction that scripts what a frontier model does unprompted, enumerates cases one stated principle covers, or steers by always/never where the reason would do. The quantifiers the doctrine tests pin are acceptance bars, not this defect.
- `docs/lessons/` carries the repository's failure records. Two carry review checks, and a violation of either is a finding: a capability the diff adopts ships either its working verification or a tracking issue (neither present is the finding), and a delivered design does not contradict what its own reading concluded, stated in the spec or the ADR that carries the decision.
- This file and the skill-writing checklist are the whole rubric, and it is closed: a lens not listed here is not applied. A skill that passes is reopened only for a failure observed in a run or a change the owner asks for; a re-read that would merely improve the text is not a finding.
