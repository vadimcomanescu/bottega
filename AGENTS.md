# bottega

Autonomous issue-to-PR runs: `/bottega:maestro` takes a task or issue to a reviewed, evidence-backed PR ready to merge, and autoreview, auto-improve, improve-codebase-architecture, discover, interview-and-capture, panel, prototype, domain-modeling, setup, triage, wayfinder, bro, research, diagnosing-bugs, handoff, teach, and writing-for-agents are also available on their own. Read `README.md` for the model. This file is the map: where each thing lives, and when to read it.

## Map

| Path | What it is | Read it when |
| --- | --- | --- |
| `CONTEXT.md` | The glossary of the run vocabulary: one meaning per term, domain only | Writing any text that names a run concept, or resolving a term dispute |
| `.claude-plugin/` | Plugin and marketplace manifests | Changing install or release metadata |
| `skills/maestro/SKILL.md` | `/bottega:maestro`, the whole run as one prompt in the owner's own words: the release answer and the model check, discovery and the spec when the work is considerable, the plan set with `codebase-design`, vertical slices and their blocking edges, the codex read of the plan and the frozen spec, the plan artifact on the owner's go, the ultracoded build working the frontier with a codex reviewer per slice, the autoreview closeout seat, the QA loop with its fixers, close; every worker's model is named in the sentence that dispatches it | Anything touches a run's flow, its state, or which model runs where |
| `skills/discover/SKILL.md` | The discovery method a run's Discover phase uses whole: understand the intent by reading the touched code, fan out Opus workers one per question, size the method to the work, then the unknowns moves (the blind spot pass, options and prototypes, the interview run through `interview-and-capture`), ending with everything settled handed to the spec, and also invocable on its own | Anything touches how a run understands a request, finds unknowns, sets scope, brainstorms, or prototypes |
| `skills/interview-and-capture/SKILL.md` | The relentless interview a run's discovery settles unknowns with: a design tree worked round by round across its frontier, each question with a recommended answer, prototypes for design questions, terms and decisions captured through `domain-modeling`, and also invocable on its own | Anything touches how questions reach the user or how settled answers are captured |
| `skills/spec/SKILL.md` | `/bottega:spec`, the spec method: synthesis only with no interview, the seam check confirmed first, the working-backwards sections from problem through the announcement to out of scope, published on the run's bead | Anything touches the spec's sections, its seam check, or where it publishes |
| `skills/prototype/` | The vendored prototype skill, synced per `THIRD_PARTY.md`: throwaway code answering one design question, a shareable HTML demo of the logic or radically different UI variants on one route, captured to a throwaway branch with its pointer on the run's bead | Anything touches how prototypes are built, judged, or captured |
| `skills/domain-modeling/` | The vendored domain modeling skill, synced per `THIRD_PARTY.md`: challenge terms against the glossary, sharpen language, concrete scenarios, cross-reference the code, update `CONTEXT.md` inline, offer ADRs under the three-condition bar, with the entry formats beside it | Anything touches glossary discipline, ADR conditions, or the entry formats |
| `skills/codebase-design/` | The deep-module vocabulary and principles every design conversation speaks in: module, interface, depth, seam, adapter, leverage, locality, with the principles that judge a design | Any module interface or seam design question |
| `skills/open/SKILL.md` | The opening method a run's Launch phase uses whole: the branch claim on the run's bead, isolated worktree, owner file and release answer, commands and the project's landing procedure from the agent map; not user-invocable | Anything touches how a run starts or claims work |
| `skills/improve-codebase-architecture/` | The vendored interactive architecture review, synced per `THIRD_PARTY.md`: scan for deepening candidates, render them as a visual HTML report, pick one, grill it through its decisions | Anything touches how improvement candidates are found, rendered, or grilled |
| `skills/auto-improve/SKILL.md` | `/bottega:auto-improve`, the headless path: run the architecture review's scan, verify the strongest candidate on the code, take it through a run | Changing how the strongest improvement is chosen or handed off |
| `skills/wayfinder/SKILL.md` | The wayfinder skill, forked from upstream and owned here (attribution in `THIRD_PARTY.md`): work too big for one session charted as an epic bead, its child decision tickets resolved one per session until the way is clear | Anything touches how oversized work is charted or worked |
| `skills/triage/` | The triage skill, its `SKILL.md` forked and owned here (attribution in `THIRD_PARTY.md`): the GitHub mailbox — CI alarms and external PRs — moved through the five-role state machine, accepted work leaving as a bead, with the agent-brief and out-of-scope formats beside it | Anything touches triage roles, states, briefs, or the out-of-scope record |
| `skills/setup/SKILL.md` | `/bottega:setup`, forked from upstream and owned here (attribution in `THIRD_PARTY.md`): beads is the tracker, so setup configures the repo around it rather than choosing one — the beads tracker doc, the branch claim, and the domain doc layout | Changing what setup writes |
| `skills/to-tickets/SKILL.md` | `/bottega:to-tickets`, forked from upstream and owned here (attribution in `THIRD_PARTY.md`): break a plan, spec, or conversation into tracer-bullet tickets published as beads with real dependency edges, each launchable as its own run once `bd ready` lists it | Changing how work becomes tickets |
| `skills/research/SKILL.md` | The vendored research skill, synced per `THIRD_PARTY.md`: a background agent reads primary sources and reports back with a URL per claim, findings kept in the session scratchpad and never committed | Anything touches how a question is researched or where findings land |
| `skills/diagnosing-bugs/` | The vendored diagnosis skill, synced per `THIRD_PARTY.md`: a feedback loop that goes red on the bug before any hypothesis, then minimise, ranked hypotheses, instrumentation, and a fix locked by a regression test, with the human-in-the-loop script beside it | Anything touches how a hard bug is diagnosed |
| `skills/handoff/SKILL.md` | The vendored handoff skill, synced per `THIRD_PARTY.md`: the conversation compacted into a document a fresh agent continues from, saved in the OS temporary directory | Anything touches how one session hands work to the next |
| `skills/teach/` | The vendored teach skill, synced per `THIRD_PARTY.md`: a stateful teaching workspace of mission, resources, lessons, reference documents, and learning records, with the format files beside it | Anything touches how the user is taught |
| `skills/writing-for-agents/` | The vendored writing reference, synced per `THIRD_PARTY.md`: how to write documents agents consume, served to installed repos while `skills-internal/write-bottega-skills` governs this repo's own prose | Anything touches the writing guidance installed repos get |
| `skills/implement` | Implementation doctrine every builder receives with its dispatched slice or repair | Changing how builders work a slice or a repair |
| `skills/use-codex` | The mechanics of running one task through codex: the dispatch through the wrapper `scripts/codex-task` beside it (one detached `codex exec` per job, job state on disk per workspace, no daemon), the orchestrator's status watch, resume and cancel, the brief, the report | Anything touches how a codex dispatch is launched, watched, recovered, or read |
| `skills/autoreview/` | The review package: `SKILL.md` is the modified vendored engine document and the whole closeout method (the engine passes, the Standards and Spec lenses, classification, fix, rerun to one converged head), self-reading the repo's `REVIEW.md` and binding its own seat models, standalone `/bottega:autoreview` on a PR, ref range, or working diff, and the run's Review phase dispatching a fresh seat to run it whole, with the helper `scripts/` and `tests/` beside it (provenance and sync in root `THIRD_PARTY.md`); see README Credits | Anything touches review method, lenses, engine mechanics, inputs, panel choice, PR threads, or merge verification |
| `skills/qa/SKILL.md` | QA method and limits: drive the shipped interface, return PASS/FAIL/NOT VERIFIED per scenario, forbidden actions; the run's QA phase routes here | Anything touches QA driving, evidence, or limits |
| `skills/close/SKILL.md` | The closing method a run's Close phase routes to: confirm the accepted head, file followups, open the PR written for a reader outside the run, braked when the launch said hold and armed only where the project's landing procedure makes arming the opener's act, watch its checks, and report it merged, ready, or waiting on what only a person can clear; not user-invocable | Anything touches PR opening, followup filing, the PR body's writing rule, the check watch, or the ready report |
| `skills/panel/SKILL.md` | `/bottega:panel`, independent drafts from different companies' models and a compare-only judge for one costly decision; a run calls it too, and it states the three conditions that make a panel worth its cost | Changing panel seats, judging, or when a panel is worth running |
| `skills/bro/SKILL.md` | `/bottega:bro`, the talk register adopted for the rest of the session: human talk, jargon-free, with technical content in Simplified Technical English; the maestro talks through it | Changing the register or where it reaches |
| `skills-internal/write-bottega-skills` | Bottega's own skill-writing doctrine, the house format, and the closing checklist; repo-only, never installed with the plugin | Creating or editing any skill file |
| `scripts/` | Bottega's own scripts, each header stating its contract: `pr-threads` (GitHub review mechanics) and `bt` (the multi-account session launcher) | Any GitHub mutation mechanics, or launching sessions across Claude accounts |
| `hooks/` | The route guard and its registration; the guard states its own policy | Changing what dispatches are denied |
| `THIRD_PARTY.md` | Every file copied from outside this repo: upstream, pinned revision, local scoping, how to sync | Reading, syncing, or adding a vendored file |
| `docs/adr/` | Append-only decision records | Understanding why a current rule exists before changing it |
| `docs/lessons/` | Failure records: what happened, the rule, and where the rule is enforced | Shaping new work in discovery or design, and when a run diagnoses a failure worth keeping |
| `tests/` | The verification gate's suites | Any change; the gate pins doctrine and script contracts |

`.agents/skills` links every skill directory; `.claude/skills` links the two a session working in this repo invokes by bare name (`autoreview`, `write-bottega-skills`). Every link points at its one copy: `skills/`, which is what the installed plugin serves, or `skills-internal/`, which holds the repo-only doctrine the plugin never ships.

## Changing this repo

- The product is skills prose read by fable and sol, held by tests. Author changes directly with the owner, in a worktree and branch. `/bottega:maestro` remains for changes to what the software does (hooks, scripts, schemas, workflows).
- Verification gate: `npm test` (the vitest suites plus the vendored autoreview Python suites, needs python3 and git on PATH). Never pipe test output inside a `&&` chain: redirect to a file and check the exit code.
- Creating or editing any skill file, load `skills-internal/write-bottega-skills`, evaluate the writing against it, and end by walking its closing checklist. Its house rules govern every file's prose in this repo, not only skills.
- Every merge to `main` is a release, per `docs/adr/0028-the-launch-decides-the-release.md`: the release gate checks the version bump and `npm test`, whoever opens a PR arms auto-merge in the same breath (`gh pr merge --auto --squash`) and watches it to the merge, the README's command table stays in sync with the skills, and the release commit is titled `bottega X.Y.Z: <headline> (#PR)`. To stop a PR landing, leave it a draft or unarmed.
- Documentation duties land in the same diff: a decision meeting `domain-modeling`'s ADR bar lands its ADR, a diagnosed failure lands its lesson in `docs/lessons/`, and every doc a change makes stale is fixed or deleted. When a decision replaces an old direction, strip the old one completely and stop: git history is the record of what was removed, and no test or doctrine line asserts a removed thing stays absent.
- PR bodies contain review-relevant content only, with no tool, model, or company attribution. Keep `CLAUDE.md` symlinked to this file.

## Review guidelines

Reviewing a change to this repository, read root `REVIEW.md` first. It carries the Bottega-specific review risks.
